{# Página inicial do plugin Menegaro Core #}
<div class="card" style="margin:16px;">
  <div class="card-header">
    <h2 style="margin:0;">Menegaro — Console</h2>
  </div>
  <div class="card-body">
    <p><strong>GLPI:</strong> {{ glpi_version }}</p>
    <p><strong>Plugin:</strong> {{ plugin_version }}</p>

    {% if is_hml %}
      <div class="alert alert-warning" role="alert" style="margin-top:8px;">
        ⚠️ <strong>AMBIENTE DE HOMOLOGAÇÃO</strong> — e-mails bloqueados / dados de teste.
      </div>
    {% endif %}

    <hr>

    <div style="display:grid; grid-template-columns:repeat(auto-fit,minmax(260px,1fr)); gap:12px;">
      <div class="card">
        <div class="card-header">
          <h3 style="margin:0;">Atalhos</h3>
        </div>
        <div class="card-body">
          <ul>
            <li><a href="{{ path('/') }}">Página inicial do GLPI</a></li>
            <li><a href="{{ path('/front/ticket.php') }}">Chamados</a></li>
            <li><a href="{{ path('/front/report.php') }}">Relatórios</a></li>
          </ul>
        </div>
      </div>

      <div class="card">
        <div class="card-header">
          <h3 style="margin:0;">Status do Ambiente</h3>
        </div>
        <div class="card-body">
          <ul>
            <li>Host: {{ hostname ?: 'não configurado' }}</li>
            <li>HML ativo: {{ is_hml ? 'Sim' : 'Não' }}</li>
          </ul>
        </div>
      </div>

      <div class="card">
        <div class="card-header">
          <h3 style="margin:0;">Próximos Passos</h3>
        </div>
        <div class="card-body">
          <ol>
            <li>Criar módulos específicos (relatórios, integrações API, etc.).</li>
            <li>Adicionar cron personalizado se necessário.</li>
            <li>Versionar e lançar release (tags Git) para PRD.</li>
          </ol>
        </div>
      </div>
    </div>
  </div>
</div>
